import React from 'react';

import { FaMarkdown } from 'react-icons/fa';
import { IconBaseProps } from 'react-icons/lib';

export default function IconMarkdown(props: IconBaseProps): React.JSX.Element {
  return <FaMarkdown {...props} />;
}